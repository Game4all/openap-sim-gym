[project]
name = "pfe-traj-opt"
version = "0.1.0"
description = "Code pour l'optimisation de trajectoires d'avion avec de l'intelligence artificielle"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gymnasium>=1.2.3",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["xpc*", "xp_sim_gym*"]